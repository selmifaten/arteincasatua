export const metadata = {
  title: "L'Arte in Casa Tua: Chi Siamo",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
     <body>{children}</body>
    </html>
  );
}
