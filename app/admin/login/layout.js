export const metadata = {
  title: "L'arte in Casa Tua: Admin Login",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
